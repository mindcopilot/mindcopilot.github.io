source "https://rubygems.org"

# Mirrors the gem set GitHub Pages uses to build the site, so a local
# build matches production.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer bundles a web server; needed for `jekyll serve`.
gem "webrick", "~> 1.8"
